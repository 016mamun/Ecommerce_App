import 'package:flutter/material.dart';

class AppColors {
  AppColors._();

  // Brand palette
  static const primary = Color(0xFF6C63FF);
  static const primaryLight = Color(0xFF9D97FF);
  static const primaryDark = Color(0xFF4840CC);

  static const secondary = Color(0xFFFF6584);
  static const accent = Color(0xFF43E97B);

  // Dark theme surfaces
  static const darkBg = Color(0xFF0F0F1A);
  static const darkSurface = Color(0xFF1A1A2E);
  static const darkCard = Color(0xFF16213E);
  static const darkBorder = Color(0xFF2A2A4A);

  // Light theme surfaces
  static const lightBg = Color(0xFFF8F9FE);
  static const lightSurface = Color(0xFFFFFFFF);
  static const lightCard = Color(0xFFFFFFFF);
  static const lightBorder = Color(0xFFE8EAF6);

  // Text
  static const textPrimary = Color(0xFFE8E8F0);
  static const textSecondary = Color(0xFF9090B0);
  static const textMuted = Color(0xFF5A5A7A);

  // Status
  static const success = Color(0xFF43E97B);
  static const warning = Color(0xFFFFA940);
  static const error = Color(0xFFFF4D4F);
  static const info = Color(0xFF40A9FF);

  // Gradient
  static const gradientStart = Color(0xFF6C63FF);
  static const gradientEnd = Color(0xFF43E97B);
  static const gradientOrange = Color(0xFFFF6584);
  static const gradientBlue = Color(0xFF667EEA);

  static const flashSaleStart = Color(0xFFFF6584);
  static const flashSaleEnd = Color(0xFFFF8E53);
}
